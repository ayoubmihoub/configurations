server:
  port: 8082

spring:
  application:
    name: user-service

  datasource:
    url: jdbc:postgresql://localhost:5432/smart-recruit
    username: postgres
    password: "***"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/smart-recruit
  mail:
    host: smtp.gmail.com
    port: 587
    username: ayoubmihoub80@gmail.com
    password: omrg trrs mcxy zigc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

keycloak:
  server-url: http://localhost:8081
  realm: smart-recruit

  client-id: smart-recruit-client
  client-secret:

  admin-realm: master
  admin-client-id: admin-cli
  admin-username: admin
  admin-password: admin

app:
  frontend-url: http://localhost:4200